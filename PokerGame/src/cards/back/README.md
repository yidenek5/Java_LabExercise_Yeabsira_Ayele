Move back images here so code can reference `src/cards/back/`.

Expected files:
- back.png
- backcomputer.png
- backcomputer.jpeg

You can copy the existing images from `src/cards/` into this folder so both paths work, or update the images in-place.
